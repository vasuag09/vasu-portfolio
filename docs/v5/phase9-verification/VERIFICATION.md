# Phase 9 Verification — Quality-Tier Veo Finals

Date: 2026-06-11 · Clips generated by Vasu in Flow (Quality tier, prompts from
VEO-BRIEF.md direction lock + Phase-9 amendments) · Contact sheets in this
folder.

## Review (checklist order: palette / loop / half-size / artifacts / mood)

| Flagship | Verdict | Notes |
|---|---|---|
| FundlyMart (cubes) | **APPROVE** | Green edge-light only on black ✓; lattice reads at half size ✓; lab mood ✓. Loop restart visible (dim start → lit lattice end) — Veo ignored "seamless loop" as the brief predicted. |
| NM-GPT (paper→beam) | **APPROVE** | The Phase-9 loop amendment worked: beam fades back into floating pages, last frame ≈ first. Full narrative arc preserved. Strongest clip of the set. |
| GeoVision (scanline) | **APPROVE** | Scanline classification = literal product truth ✓; classified zones brighter than the Fast version but stay muted-cartographic with green edges. Loop restart visible. |

Source resolution 1280×720 @ 24fps — identical to the shipped Fast-tier
encodes (Flow output class), and sufficient for the ~700px case-study slot.

## Encode results (spec: ≤4MB per source, no audio)

| Clip | AV1 | HEVC (hvc1) | H.264 | Poster |
|---|---|---|---|---|
| fundlymart | 1.3M | 1.7M | 3.9M | 8.6K |
| nm-gpt | 2.6M | 3.4M | 3.3M (CRF 27 — 21 produced 7.8M; dust = H.264 worst case) | 16.6K |
| geovision | 0.8M | 1.2M | 2.5M | 34.3K |

All ≤4MB ✓. Filenames unchanged → `veo-sources.ts` / `<VeoClip>` wiring
untouched.

## 4G throttle test (exit criterion: no buffering)

Method: production build, CDP `emulateNetworkConditions` 4 Mbps down / 60ms
RTT, deep-link `?project=` per flagship, clip scrolled into the lazy-mount
window, one full 8s loop observed.

| Clip | Served source | Startup | Full loop | Mid-play stalls |
|---|---|---|---|---|
| fundlymart | av1 | ~0.7s | ✓ (wrapped to 0.32s after 9s) | 0 |
| nm-gpt | av1 | ~0.5s | ✓ (wrapped to 0.51s) | 0 |
| geovision | av1 | ~0.4s | ✓ (wrapped to 0.43s) | 0 |

(`waiting` events fired only during initial buffer; elapsed-vs-currentTime
math confirms continuous playback through the loop point.)

## Budget

Phase 4 spent ~60cr (6 Fast). Phase 9: 3 Quality generations, no retries
needed (~300cr nominal) → ≈360 total, under the 400 ceiling. **Vasu:
glance at the Flow balance to confirm the actuals.**

## Housekeeping

Root-level Flow downloads (9 files, Fast + Quality) are now gitignored
(`/*.mp4`) and stay local as reference per VEO-BRIEF.md. Encoded deliverables
live in `app-v5/public/veo/`.

## Carried

- Loop-restart seams on fundlymart/geovision: acceptable as ambient payload;
  if Phase 11 polish wants them gone, options are a 0.3s CSS crossfade in
  VeoClip or `loop=false` + hold-on-last-frame. Decide at self-score.
